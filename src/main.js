import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app') //this is a selctor that selects the div with id app that is in index.html and it takes in App which is a vue js component as specified on line 3

//our focu is now not on index.html or main.js but instead app.vue
